export type PublicProduct = {
  id: number
  name: string
  brand: string
  category: string
  target_users: string
  price: number
  processor: string
  ram: string
  storage: string
  gpu: string
  thumbnail: string | null
}
